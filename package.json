{
  "name": "@cgs/token",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.3.0",
    "hardhat": "^2.23.0"
  },
  "scripts": {
    "start-localhost-network": "npx hardhat node --port 7545",
    "send-eth-localhost": "npx hardhat --network localhost run scripts/send-eth-localhost.ts"
  }
}
